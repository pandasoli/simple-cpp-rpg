#pragma once
#include <string>
#include <vector>
#include "../types.h"

using namespace std;


int random(int, int);
int abs(int num);
vector<Pos> makeLine(Pos from, Pos to);
string inttostr(int number);

